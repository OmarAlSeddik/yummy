import FilteredMeals from "@/components/FilteredMeals";
import { type NextPage } from "next";

const FilteredMealsPage: NextPage = () => {
  return <FilteredMeals />;
};

export default FilteredMealsPage;
